import SignUpCard from "./SignUpCard";
export const metadata = {
    title: "Sign Up",
    description: "Give your details and get start with devhub.",
};

const SignUp = () => {
    return (
        <div>
            <SignUpCard />
        </div>
    );
};

export default SignUp;